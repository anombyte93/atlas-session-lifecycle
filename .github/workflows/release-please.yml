name: Release Please

on:
  push:
    branches: [main]

jobs:
  release:
    uses: anombyte93/copilot/.github/workflows/reusable-release-please.yml@v1
    permissions:
      contents: write
      pull-requests: write
